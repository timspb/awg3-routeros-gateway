# RouterOS container adapter

This directory stores the generated installer template and deployment metadata for the `linux/arm/v5` ordinary `/container` flow.

The runtime contract stays identical to the arm64 app build.

